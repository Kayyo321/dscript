def maybe_named(value) (name: $of_a_block) {
    log('== named blocks and none ==');
    log(value);

    if name is none then {
        log('name is none');
    } else {
        name();
    }

    if $of_a_block is none then {
        log('$of_a_block is none');
    } else {
        $of_a_block();
    }
}

fn main() {
    maybe_named 10;

    maybe_named 20 name: {
        log('named block ran');
    }
}
