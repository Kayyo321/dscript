class Box {
    fn init(v) {
        self.value = v;
    }
}

fn main() {
    log('== object indexing keys ==');

    let b = Box(42);
    log(b['value']);

    let list = [10, 20, 30];
    log(list[1]);

    let text = 'xyz';
    log(text[0]);
}
