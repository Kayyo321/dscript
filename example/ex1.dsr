from 'std' import 'math';
"You could also do: from '.' import 'other-file'"

fn main() {
    log('Hello, World!');

    funny(let i = 10) {
        log('This is the block-literal!');
        log(i);
    }
}

"This is a comment"
fn funny(%expr_literal) ($block_literal) {
    %expr_literal();
    $block_literal();
}

def dummy; "simple custom keyword, no logic attached"

def smarty(cond) {
    if cond then {
        log('true!');
    } else {
        log('*loud incorrect buzzer*!');
    }
}